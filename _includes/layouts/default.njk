<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{title}}</title>
        <link rel="stylesheet" href="/assets/styles.css">
    </head>
    <body>
        <div class="container">
            <header id="title" class="stack">
                <div class="box">
                    <h1>{{title}}</h1>
                    <span class="font-size:lg">{{subtitle}}</span>
                </div>
            </header>
            <div class="sidebar">
                <aside>
                    {% include "partials/components/sidebar.njk" %}
                </aside>
                <main>
                    {{content | safe}}
                </main>
            </div>
        </div>
    </body>
</html>